package com.shining.webhandler.common

/**
 * Enum.kt
 * WebHandler
 */

enum class FragmentType {
    Dashboard,
    WebView,
    Collection,
    Setting
}

enum class ImageType {
    PNG,
    JPG,
    JPEG,
    GIF
}