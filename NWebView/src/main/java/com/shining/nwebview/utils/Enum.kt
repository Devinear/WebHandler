package com.shining.nwebview.common

/**
 * Enum.kt
 * WebHandler
 */
enum class DownloadSupportType {
    PNG,
    JPG,
    JPEG,
    ZIP,
    XLS
//    ".png",
//    && !name.contains(".zip") && !name.contains(".xls")
//    && !name.contains(".jpg") && !name.contains(".jpeg")
//    ) {
}